<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/bg_card"
        android:theme="@style/ToolbarTheme"
        app:titleTextColor="@color/gold" />

    <!-- Search Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_margin="8dp"
        android:background="@drawable/bg_input"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="8dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔍"
            android:textSize="16sp" />

        <EditText
            android:id="@+id/etSearch"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:background="@null"
            android:hint="Search bill number or customer..."
            android:textColorHint="@color/white38"
            android:inputType="text"
            android:textColor="@color/white"
            android:imeOptions="actionSearch" />
    </LinearLayout>

    <!-- Filter Chips -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none">
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingBottom="8dp">

            <TextView android:id="@+id/chipAll"
                style="@style/Chip" android:text="ALL" />
            <TextView android:id="@+id/chipGold"
                style="@style/Chip" android:text="GOLD" />
            <TextView android:id="@+id/chipSilver"
                style="@style/Chip" android:text="SILVER" />

            <View android:layout_width="16dp" android:layout_height="1dp"/>

            <TextView android:id="@+id/chipOpened"
                style="@style/Chip" android:text="OPENED" />
            <TextView android:id="@+id/chipClosed"
                style="@style/Chip" android:text="CLOSED" />
            <TextView android:id="@+id/chipAllStatus"
                style="@style/Chip" android:text="ALL" />
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Total bar -->
    <TextView
        android:id="@+id/tvTotal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/bg_card"
        android:padding="10dp"
        android:text="0 bills found"
        android:textColor="@color/white54"
        android:textSize="13sp" />

    <!-- Progress -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="32dp"
        android:indeterminateTint="@color/gold"
        android:visibility="gone" />

    <!-- Empty -->
    <TextView
        android:id="@+id/tvEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="32dp"
        android:text="No bills found"
        android:textColor="@color/white54"
        android:visibility="gone" />

    <!-- List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp"
        android:clipToPadding="false" />

    <!-- Load More -->
    <Button
        android:id="@+id/btnLoadMore"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:backgroundTint="@color/gold"
        android:text="Load More"
        android:textColor="@color/bg_dark"
        android:visibility="gone" />

</LinearLayout>
